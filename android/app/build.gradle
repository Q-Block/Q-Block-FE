plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

// Access environment variables directly
def flutterVersionCode = System.getenv('FLUTTER_VERSION_CODE') ?: '1'
def flutterVersionName = System.getenv('FLUTTER_VERSION_NAME') ?: '1.0'

android {
    namespace = "com.example.qblock_fe"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    defaultConfig {
        applicationId = "com.example.qblock_fe"
        minSdk = 23
        targetSdk = flutter.targetSdkVersion
        versionCode = flutterVersionCode.toInteger()
        versionName = flutterVersionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
            buildConfigField "String", "NAVER_MAP_CLIENT_ID", "\"ub6c5z1yy9\""
        }




    }
}

dependencies {
    implementation "androidx.work:work-runtime:2.7.1" // WorkManager 최신 버전 추가
}

flutter {
    source = "../.."
}

